[project]
name = "ticket-reader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.6",
    "pillow>=12.2.0",
    "tinygrad",
]

[tool.uv.sources]
tinygrad = { path = "../tinygrad", editable = true }
